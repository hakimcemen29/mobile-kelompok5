<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.test">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Test"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Test" />

        <!-- Tambahkan RegisterActivity di sini -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:label="Register"
            android:theme="@style/Theme.Test" />

        <activity
            android:name=".HomePage"
            android:exported="false"
            android:label="Home Page"
            android:theme="@style/Theme.Test" />

        <!-- Hapus intent-filter dari DataEntryActivity -->
        <activity
            android:name=".DataEntryActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Test" />

        <!-- Tambahkan MeaningfullActivity sebagai launcher -->
        <activity
            android:name=".MeaningfullActivity"
            android:exported="true"
            android:label="Meaningfull Activity"
            android:theme="@style/Theme.Test">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
